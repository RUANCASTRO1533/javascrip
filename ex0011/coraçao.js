
function curar() {
    var fin = document.getElementById("in").value
    var ini = 0
    ini = Number(ini)
    fin = Number(fin)
    while ( ini < fin  ){
        document.getElementById("aprv").innerHTML += `<img src="/img/png-transparent-drugs-marijuana-cannabis-drug-hemp-leaf-plant-pot-weed-silhouette.png" width="100px" height="100px">`
        ini++
    }
}

function curar() {
    var fin = document.getElementById("in").value
    var ini = 0
    ini = Number(ini)
    fin = Number(fin)
    while ( ini < fin  ){
 
        ini++
    }
}

